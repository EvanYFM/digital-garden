# 设计系统文档 · 小马的个人网站

**风格定位**：现代东方极简主义（Modern Oriental Minimalism）
70% 东方的留白、虚实、呼吸感与哲学性的静 · 30% 现代主义的网格秩序与功能清晰
像一本持续修订的个人思想出版物，不是作品集、不是 SaaS、不是新中式。

---

## 一、设计原理（Rationale）

1. **东方感不靠具象符号**：没有水墨、山水、祥云、书法海报。东方性只通过留白、虚实、疏密、黑白反转、方圆关系、空间呼吸来表达。
2. **太极退入交互**：全站唯一的"太极"是一枚 24px 的日夜切换按钮——常态单环一点，悬停显现 S 形两仪，点击内盘旋转 180° 黑白互根。以小见大，不做任何大面积太极展示。
3. **方就是方，圆就是圆**：禁圆角卡片、禁柔和模糊 UI。当前版本几何语言进一步收敛为**圆形优先**——圆环、圆点、细线；方形仅保留品牌印章「马」一处。
4. **内容像出版物**：无阴影、无渐变堆叠。层级只靠留白、细线、字重、透明度与局部黑白反转建立。
5. **体验区原则（2026-08-18 新增）**：结构化的分析区（目录/编号/分类/高亮）之外，允许存在"不可分析区"——反结构页面：无目录、无编号、无进度、无强调层级，交互只对"静止"作出回应。理性是工具不是归宿；有些内容不能被设计成可检索的，只能被进入。
5. **动效如呼吸**：淡入、细线展开、微位移、小范围悬停反馈。不做视差炫技。

---

## 二、色彩 Tokens

| Token | 日间（纸） | 夜间（墨） | 用途 |
|---|---|---|---|
| `--paper` | `#f6f4ee` | `#141412` | 页面底色（纸白 / 深墨，不用纯黑） |
| `--paper-soft` | `#efece4` | `#1b1b18` | 次级底色（边注块、悬停态） |
| `--ink` | `#1c1c1a` | `#e9e6de` | 主文字 / 主图形（墨黑 / 纸白） |
| `--charcoal` | `#2b2b28` | `#d6d3ca` | 正文文字（炭灰，柔于主文字） |
| `--gray` | `#8a877d` | `#7d7a71` | 辅助信息、英文副层、元信息 |
| `--line` | `rgba(28,28,26,.16)` | `rgba(233,230,222,.16)` | 分隔细线 |
| `--line-strong` | `rgba(28,28,26,.4)` | `rgba(233,230,222,.42)` | 强调线、引文竖线 |
| `--cinnabar` | `#b3402a` | `#c8503a` | **朱砂，印章式点缀，极度克制** |

**朱砂使用白名单**（全站仅这些位置）：品牌印章「马」、章节序号、特写标记、价值排序首位圆点、状态点、篇末印记圆环、面包屑分隔符、观页经注入口圆点（来源行「书名●章节」中缝的朱砂实心点，2026-09-02 小马确认新增）。新增使用点需重新评估稀缺性。

**明暗模式**：`html[data-theme]` 切换，CSS 变量整体过渡 0.9s，`localStorage('theme')` 持久化。日间=纸白底墨字；夜间=深墨底纸字（避免纯黑刺眼）。纯黑纯白只用于关键对比瞬间。

---

## 三、字体与字阶

| 角色 | 字体栈 | 场景 |
|---|---|---|
| 衬线 `--serif` | Noto Serif SC → Songti SC → SimSun | 标题、姓名、引文、条目名、章节序号 |
| 无衬线 `--sans` | Noto Sans SC → PingFang SC → 雅黑 | 正文、导航、标签、元信息、UI |

**字阶**（桌面端）：
- 页面主标题：clamp(40px, 6vw, 64px) / serif 600 / 字距 .22em
- 文章标题：clamp(28px, 3.6vw, 40px) / serif 600
- 板块标题：26px / serif 600 / 字距 .16–.2em
- 章节标题 h2：23px / serif 600 / 字距 .1em
- 条目标题：17–20px / serif 600
- 正文：15px / 行高 2.3 / 字距 .04em / 两端对齐
- 辅助正文：13–14px / 行高 2–2.3
- 元信息：11px / 字距 .12–.2em
- 英文副层：9.5–12px / 字距 .24–.44em / 全大写 / 灰色

**语言分层**：中文为主内容层；英文为编辑结构副层（如「思考 / NOTES」），小号、大写、宽字距、灰色。

---

## 四、间距与栅格

- **内容最大宽度**：1080px（列表页）/ 1180px（带目录页）；正文栏 660px（文章页最佳行长）
- **页头呼吸区**：padding-top 200–220px，页头下留白 96–110px
- **板块间距**：section padding 88–120px；板块间以 1px `var(--line)` 分隔
- **条目内节奏**：条目纵向 padding 26–34px；网格单元 padding 32–48px
- **栅格**：列表条目 `56px / 1fr / 150px / 110px` 四栏（编号/标题/分类/日期）；研究变量格 3 列；关注方向 2×2
- **移动端**（≤760px）：一律降单栏，英文副层隐藏，目录隐藏（≤960px）

---

## 五、边界与线的规则

- 所有分隔只用 **1px 细线**，颜色 `var(--line)`；强调处用 `var(--line-strong)`
- 网格用**共边细线**（单元格 border 拼合），不用卡片间距
- 引文块：1px 左侧竖线 + 内缩 36px
- **禁止**：投影、圆角卡片、渐变背景、毛玻璃拟物（仅导航栏允许 `backdrop-filter` 8px 以保证可读性）

---

## 六、几何语言（圆 / 点 / 线）

| 元素 | 语义 | 用法 |
|---|---|---|
| 圆环（空心） | 标记、节点、状态位 | 特写标记、价值排序、探索领域、more-link |
| 圆点（实心） | 当下、注意、激活态 | 悬停墨点、目录当前位置、状态点 |
| 细线 | 关系、路径、分界 | 板块分隔、连接线、目录竖轨 |
| 朱砂圆点/环 | 印章式强调 | 全站白名单内使用 |

**交互惯例**：悬停条目时，标题右移 8–10px + 墨点浮现；圆环节点悬停填充为墨或朱砂。

---

## 七、核心组件

1. **导航 header**：左印章+姓名，右五栏链接+太极日夜钮；次级页带 86% 透明底+8px 模糊+底部分隔线。当前项/悬停以 1px 下划线展开表示。
2. **太极日夜钮 `.taiji`**：常态=单环+中心一点；悬停=S 形两半+两仪点浮现（旋入）；点击=内盘 180° 旋转，黑白互换。全站共用同一份 SVG 与样式。
3. **页头 `.page-head`**：面包屑（朱砂分隔符）→ 衬线大标题 → 英文副层 → 左竖线引文。
4. **文章条目 `.row` / `.entry`**：编号 / 标题 / 分类 / 日期；悬停墨点+标题位移。
5. **sticky 目录 `.toc`**：左侧 200px，`position:sticky; top:160px`；一条静止竖轨 + 游走墨点；滚动监听在视口 28% 高度处切换当前章节（简体数字编号一/二/三…）。
6. **引文块 `.pull`**：左竖线 + 衬线大字 + 灰色小注。
7. **边注 `.sidenote`**：`--paper-soft` 底块，小标题宽字距。
8. **篇末印记 `.endmark`**：两条细线夹一枚朱砂圆环。
9. **上一篇/下一篇 `.pn`**：双栏，中间 1px 分隔，悬停标题左右微移。
10. **循环示意 `.loop`**（复盘页）：思考→执行→复盘→修正，圆点节点+细线连接。
11. **状态点**（项目页）：朱砂实心小点 + 「运行中」。

---

## 八、动效原则

- **入场**：IntersectionObserver 触发 `.rv` → 透明度 0→1 + 上移 26px→0，时长 1.1s，`cubic-bezier(.22,1,.36,1)`；级联延迟 .12s/.24s/.36s
- **呼吸**：首页"一环一点"标记 9s 呼吸缩放（1→1.03）；轨道点 48s 缓行一周
- **细线展开**：板块标题右侧线 scaleX 0→1，1.4s
- **悬停**：均在 0.4–0.6s 内完成，位移 ≤10px
- **禁忌**：无视差滚动、无大面积位移、无循环播放的醒目动画（轨道点除外，它象征时间）

---

## 九、信息架构与页面

```
index.html      首页      封面（一环一点+姓名+身份陈述）→ 精选思考3条 → 关注方向4格
notes.html      思考      页头 → 此刻入口条 → 观入口条 → 特写（纯排版）→ 条目索引（生长状态：常青●/生长◍/种子○）→ 随记（碎片捕捉层，localStorage `fragments`，Ctrl+Enter 种下，悬停可移除）
research.html   研究      四条准则 → 宏观/期货/AI三主线 → 初步探索六领域
projects.html   项目      编辑式大条目 ×3（宏观工作台/期货工作站/A股仪表盘）+ 状态点
review.html     复盘      循环示意（思考→执行→复盘→修正）→ 每日此刻索引（私密区块：仅已设置同步 Token 的设备显示；状态为 今天·可修改/已封存/当日未记录）→ 周/月/季复盘索引
about.html      关于      个人操作系统：使命/价值排序/决策算法/认知/学习/情绪/复盘机制/修心（v0.2 · 2026Q3）
now.html        此刻      每日记录：状态三项（精力/专注/感受）+ 六字段；localStorage 持久化 + 私有仓库同步（garden-data.js，本地优先，无 Token 退化为纯本地）；当天可改，次日永久封存（空日亦封存）；支持 ?date= 打开指定日；导出/导入 JSON 备份；跨午夜自动翻日；**私密锁**：6 位密码门面（可被绕过，不承载数据安全）+ 私有仓库 Token 才是真正边界
article.html    文章页    数据驱动模板：按锚点路由渲染——`article.html#a0`~`#a7`（`?id=` 为后备；锚点在 file:// 与静态服务下均可靠），共 8 篇（1 篇特写 + 7 篇思考条目），每篇含简介 / 3-4 章节 / 引文·边注 / 上下篇导航；sticky 目录 + 滚动高亮；数据集中在顶部 ARTICLES 与 ORDER
jing.html       观        体验区：无目录/无编号/无进度/无页脚，单圆缓息；句子等权重轮转，鼠标静止 1.4s 才浮现，移动即退去；一句话提示静止后永久消失；道德经/金刚经语句带经注入口——悬停不退、来源行「书名●章节」中缝朱砂圆点提示、点击跳 classics/ 对应章；语录只收道家/佛家禅语/西方哲学（儒家已于 2026-09-02 剔除，庄子保留）
classics/       经注      古文经典解读：道德经全本注解（81 章）/ 金刚经全本解读（32 分），原样收录（楷体纸色自有风格），工具栏首钮「观」返回；深链格式 `daodejing.html#章号` / `jingangjing.html#分号`
```

**导航**：思考 Notes / 研究 Research / 项目 Projects / 复盘 Review / 关于 About + 太极日夜钮。

---

## 十、内容呈现规则

- 所有内容标注日期与修订次数，"保留修正自己的权利"是全站叙事母题
- 条目编号用简体中文数字（一二三四…），不用大写（壹贰叁肆）、不带冗余圆圈前缀
- 研究内容四准则：先确认口径、先识别主要矛盾、事实/推断/反例三分、非投资建议
- 图片原则：界面不依赖装饰图；特写条目以纯排版呈现（"以字为图"）；如未来引入摄影/生成图，低频、大幅、编辑式，保留自然色
- 页脚固定：左「克制 · 清明 · 长期主义」，右版权+英文小字

---

## 十一、实现备注

- 当前为静态 HTML（8 个页面 + 共享数据层 `garden-data.js`，CSS/JS 内联，零依赖），字体走 Google Fonts CDN
- 工程化方向：Next.js + Tailwind，把 `:root` 变量映射为 design tokens；`.taiji`、`.toc`、`.rv` 抽为组件；文章数据迁移至 MDX/Content Collections
- 暗色模式务必两套变量同步维护；新增颜色先问"能否用透明度解决"

## 十二、数据层与部署

- **双仓库**：`EvanYFM/digital-garden`（公开，站点与页面）/ `EvanYFM/digital-garden-data`（私有，此刻与随记数据）
- **同步机制**：`garden-data.js` 提供 pull/sync（GitHub Contents API，409 冲突自动重试一次）；合并规则——now 按日期（远端为基准、本地独有保留、今天本地优先），fragments 按时间戳并集
- **本地优先**：无 Token 时全部退化为纯 localStorage，不报错、不阻塞
- **部署**：GitHub Pages，main 分支根路径，push 后约 1 分钟生效；`.nojekyll` 必须保留
- **私密边界三层**：① 此刻页 6 位密码门（前端，可被绕过，只挡随手点击）② 私有仓库（数据本体，匿名 404）③ Token（仅本人设备持有）。真正的墙是 ②③；① 是门面
